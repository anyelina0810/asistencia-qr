{
  "name": "qr-attendance-system",
  "version": "1.0.0",
  "description": "Sistema de Control de Asistencias mediante Códigos QR — Entorno Universitario",
  "main": "backend/server.js",
  "scripts": {
    "start":   "node backend/server.js",
    "dev":     "nodemon backend/server.js",
    "db:reset": "rm -f database/attendance.db && node -e \"require('./backend/config/database').initDB().then(() => { console.log('DB recreada.'); process.exit(0); })\""
  },
  "keywords": ["qr", "attendance", "university", "education"],
  "license": "MIT",
  "dependencies": {
    "bcryptjs":           "^2.4.3",
    "better-sqlite3":     "^9.4.3",
    "cors":               "^2.8.5",
    "express":            "^4.18.3",
    "express-rate-limit": "^7.2.0",
    "helmet":             "^7.1.0",
    "jsonwebtoken":       "^9.0.2",
    "morgan":             "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
